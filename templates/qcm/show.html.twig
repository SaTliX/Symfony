{% extends 'base.html.twig' %}

{% block title %}QCM{% endblock %}

{% block body %}
    <h1>QCM</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ q_c_m.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ q_c_m.Title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ q_c_m.Description }}</td>
            </tr>
            <tr>
                <th>Time</th>
                <td>{{ q_c_m.Time ? q_c_m.Time|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_q_c_m_index') }}">back to list</a>

    <a href="{{ path('app_q_c_m_edit', {'id': q_c_m.id}) }}">edit</a>

    {{ include('qcm/_delete_form.html.twig') }}
{% endblock %}